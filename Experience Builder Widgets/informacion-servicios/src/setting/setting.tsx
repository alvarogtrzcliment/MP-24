import { type AllWidgetSettingProps } from 'jimu-for-builder'
import { React } from 'jimu-core'

const Setting = (props: AllWidgetSettingProps<any>) => {
  return (
    <div>
      <h2>Esta es la zona de settings</h2>
    </div>
  )
}

export default Setting
